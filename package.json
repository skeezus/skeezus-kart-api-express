{
  "name": "api2",
  "version": "1.0.0",
  "description": "* enter project directory * if project exists clone repository; if not run `npm init && npm install` * run `docker-compose up --build` to create image and configure server environment",
  "main": "api/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "csv-parser": "^2.3.3",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "knex": "^0.21.2",
    "multer": "^1.4.2",
    "objection": "^2.2.2",
    "objection-password": "^2.0.0",
    "pg": "^8.3.0",
    "strip-bom-stream": "^4.0.0"
  }
}
